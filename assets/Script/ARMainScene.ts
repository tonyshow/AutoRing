import Scene from "../Framework/Interface/Scene/Scene";

const {ccclass, property} = cc._decorator;
@ccclass
export default class ARMainScene extends Scene {
    // onLoad () {}
    // start () {}
    // update (dt) {}
}
